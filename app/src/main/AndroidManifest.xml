<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jaypal.navigation_drawer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".signup"
            android:theme="@style/AppTheme.loginActionBar"></activity>
        <activity android:name=".login"
            android:theme="@style/AppTheme.loginActionBar"/>
        <activity android:name=".splashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".doctor_detail_final"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".comapare_final"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".comparelist"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Medicine_centers_in_desti"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".tratement_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".medicine_center_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".destination_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>